(defvar var_perfmode "Quiet")

(defwindow perfmodes
  :geometry (geometry
              :width "400px"
              :height "400px"
              :anchor "bottom right")
  (label :text "test"))

(defwidget cb_perfmode[text]
  (button
    (label
      :class "icon")
    (label
      :text text)))
